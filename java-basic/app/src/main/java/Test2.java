// 루트 패키지에 소속된 경우에는 package 를 지정할 필요가 없다.

class Test2 {
    // 다른 패키지에 소속된 클래스를 사용할 경우에는 패키지명까지 적는다.

//    com.eomcs.F1 v1; // 컴파일 오류 : 다른 패키지에서 접근 불가
//    com.eomcs.F2 v2; // 컴파일 오류 : 다른 패키지에서 접근 불가

    com.eomcs.G1 v3; // public 인 경우 다른 패키지에서 접근 가능
//    com.eomcs.G2 v4; // 컴파일 오류 : 공개되지 않은 클래스
}
